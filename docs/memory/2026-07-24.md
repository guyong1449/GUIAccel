# 2026-07-24

## Summary

- Switched the speculative decoding reproduction source to the existing
  `/dkucc/home/rw335/MSD` checkout at commit
  `fd76a5ee2bd107a5a04f05afd0651aab7ba6fab4`.
- Confirmed MSD supports Qwen2-VL-7B through `EaModel.msdgenerate`; it does not
  provide Qwen3-VL weights or an AndroidControl adapter.
- Removed GUIAccel's AndroidWorld-only source, launchers, test, configuration,
  canonicalization, aliases, and documentation references.
- Added a lazy-loaded MSD-Qwen2-VL backend, AndroidControl evaluation CLI,
  config, setup script, Slurm launcher, and unit tests.

## Verification

- `python -m py_compile ...`: passed.
- `pytest -q tests/test_msd_qwen2vl_backend.py`: 2 passed.
- AndroidControl TFRecord dry-run: loaded test episode 140 and produced both
  high-level and low-level requests.
- Remaining source search for `androidworld|android_world`: no matches.

## Blockers

- Proxy bridge `127.0.0.1:17890` is not listening, so the isolated environment
  and Hugging Face model weights cannot yet be downloaded.
- Current Codex filesystem permissions do not permit creating `/work/rw335`
  output directories; the launcher retains `/work` as the real-job default.

## Later checkpoint

- User approved a Qwen2-VL-only phase: compare target-only `naivegenerate`
  against `msdgenerate` on identical AndroidControl requests; defer Qwen3-VL.
- Vendored the required Qwen2-VL MSD inference core into
  `guiaccel/model/msd/`, preserved the upstream Apache 2.0 license and source
  commit, and removed the runtime `sys.path` dependency on `/MSD/EAGLE`.
- Added `requirements-msd-qwen2vl.txt`; setup now installs GUIAccel plus the
  vendored runtime rather than installing MSD/EAGLE externally.
- Resource snapshot: login node, 12 logical CPUs, about 54 GB available RAM,
  no GPU; GPU work must use Slurm.
- `msd-androidcontrol` now appears in `conda env list`, but it is incomplete:
  `torch` is not installed and vendored MSD import fails for that reason.
- New config tests still pass in the base environment: `2 passed`.

## Continuation checkpoint

- Verified the dedicated `msd-androidcontrol` environment now contains
  torch 2.1.2+cu121, transformers 4.48.3, and accelerate 1.3.0; the vendored
  `EaModel` import succeeds.
- Found the remaining runtime dependency gap: importing `qwen_vl_utils` fails
  because `torchvision` is absent. Added the matching
  `torchvision==0.16.2` pin to `requirements-msd-qwen2vl.txt`.
- The managed Codex shell could not install that package because it cannot
  resolve PyPI. No compatible wheel, conda package, or existing environment
  copy was found locally.
- Extended MSD request metrics to record end-to-end latency and peak allocated
  GPU memory. Extended the evaluator summary with generated tokens, total
  verification rounds, accepted draft tokens, and per-instruction-mode
  accuracy, parse-failure, latency, and token metrics.
- Verification: Python compilation passed, the dedicated MSD tests now report
  `3 passed`, launcher shell syntax passed, and the 16-request A40 Slurm
  dry-run rendered correctly.
- Neither Qwen2-VL target nor MSD draft model cache was found under
  `/work/rw335`. No Slurm job was submitted.
- User installed torchvision 0.16.2+cu121; the complete torch, torchvision,
  qwen-vl-utils, transformers, and accelerate import check now passes.
- Confirmed the official `Lyn-Lucy/MSD` README points to
  `lucylyn/MSD-Qwen2VL-7B-Instruct` on Hugging Face; GitHub does not host the
  large checkpoint itself.
- Added `scripts/setup/download_msd_models_home.sh` to cache both the official
  Qwen target and MSD draft under the user's Hugging Face home cache through
  the requested `https://127.0.0.1:17890` proxy. The managed Codex shell cannot
  see that loopback listener, so the script must be started from the user's
  proxy-capable interactive shell.
