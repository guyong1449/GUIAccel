[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "guiaccel"
version = "0.1.0"
description = "GUI Agent Inference Acceleration — decode optimization experiments."
requires-python = ">=3.9"

[tool.setuptools.packages.find]
include = ["guiaccel*"]
